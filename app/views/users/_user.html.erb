<li>
	<%= gravatar_for user, :size => 30 %>
	<%= link_to user.name, user %>
	<% if current_user.admin? %>
	|
	<%= link_to "Delete", users_path, :method => :delete, :confirm => "You Sure Dummy?", :title => "Delete #{user.name}", %>
	<% end %>
</li>
